# requirements.txt for transcription-gui
# Python version: 3.8.20
# Install with: pip install -r requirements.txt

pretty_midi
music21
spleeter==2.3.2
ffmpeg-python
mido
tqdm
apache-beam  # Only needed if training a model